//Make this correct

/*int rollno[10];
int i;

printf("Enter the roll no.");

for(i=0;i<=9;i++)
{
	scanf("%d", &rollno[i]);
}
for(i=0;i<=9,i++)
{
	printf("%d\n", rollno[i]);
}
signed/ unsigned variables
+-/+-10
*/


/*
#include<stdio.h>
int main()
{
//	printf("Hello world\n");
int marks[4];    //Declaring an variable
marks[0] = 34;   //Declaring an array
printf("Marks of student 1 is %d\n", marks[0]);

//marks[0] = 34;
marks[0] = 24;   //Declaring an array
printf("Marks of student 2 is %d", marks[0]);
	return 0;
}
*/

/*
#include<stdio.h>
int main()
{
	int marks[4];

for(int i = 0; i < 4; i++)
{
	printf("Enter the value of %d element of the array\n\n", i);
	scanf("%d", &marks[i]);
}
for(int i = 0; i < 4; i++)
{
	printf("The value of %d element of the array is %d\n", i, marks[i]); 
	
	scanf("%d", &marks[i]);
}
}

*/
